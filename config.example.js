/* Optional email API configuration.
   If you have a secure backend endpoint, copy this file to config.js and set:
   window.RF_CONFIG = { emailApiUrl: "https://your-domain.example/api/send-email.php" };
   Without an API endpoint, the approval workflow opens the user's mail app with a pre-filled email.
*/
window.RF_CONFIG={emailApiUrl:""};
